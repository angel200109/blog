import{_ as i}from"./chunks/plugin-vue_export-helper.DlAUqK2U.js";import{c as a,o as e,X as n}from"./chunks/vendor-vueuse.XPft87tp.js";const E=JSON.parse('{"title":"SQL 基础与优化","description":"","frontmatter":{},"headers":[],"relativePath":"mysql/sql-basics-and-optimization.md","filePath":"mysql/sql-basics-and-optimization.md","lastUpdated":1786990830000}'),l={name:"mysql/sql-basics-and-optimization.md"};function t(d,s,o,p,h,r){return e(),a("div",null,s[0]||(s[0]=[n(`<h1 id="sql-基础与优化" tabindex="-1">SQL 基础与优化 <a class="header-anchor" href="#sql-基础与优化" aria-label="Permalink to &quot;SQL 基础与优化&quot;">​</a></h1><h3 id="sql-分类与约束" tabindex="-1">SQL 分类与约束 <a class="header-anchor" href="#sql-分类与约束" aria-label="Permalink to &quot;SQL 分类与约束&quot;">​</a></h3><ul><li><strong>DDL</strong>：<code>CREATE</code> / <code>ALTER</code> / <code>DROP</code>，改表结构、索引、库</li><li><strong>DQL</strong>：<code>SELECT</code></li><li><strong>DML</strong>：<code>INSERT</code> / <code>UPDATE</code> / <code>DELETE</code></li><li><strong>DCL</strong>：<code>GRANT</code> / <code>REVOKE</code>。<code>COMMIT</code> / <code>ROLLBACK</code> 属于事务控制（TCL），有的资料归到 DCL</li></ul><p>常见约束：</p><ul><li><strong>主键</strong>：唯一标识一行，非空，一张表一个</li><li><strong>外键</strong>：引用另一表主键。InnoDB 支持；高并发系统常在应用层保证，避免级联锁把多张表锁在一起</li><li><strong>唯一</strong>：允许 NULL</li><li><strong>默认值</strong>、<strong>NOT NULL</strong></li><li><strong>CHECK</strong>：MySQL 8.0.16+ 才真正生效</li></ul><h3 id="子查询" tabindex="-1">子查询 <a class="header-anchor" href="#子查询" aria-label="Permalink to &quot;子查询&quot;">​</a></h3><p>一个查询的结果参与另一个查询：</p><ul><li><strong>标量</strong>：返回一个值，<code>WHERE age = (SELECT MAX(age) FROM user)</code></li><li><strong>列</strong>：n 行 1 列，配合 <code>IN</code> / <code>ANY</code> / <code>ALL</code></li><li><strong>行</strong>：1 行 n 列，<code>WHERE (age, city) = (...)</code></li><li><strong>表</strong>：n 行 n 列，放在 <code>FROM</code> 里当派生表</li></ul><p>能改写成 JOIN 时，很多场景 JOIN 更好优化。相关子查询每一行都执行一次内层，数据量大时要小心。</p><h3 id="join" tabindex="-1">JOIN <a class="header-anchor" href="#join" aria-label="Permalink to &quot;JOIN&quot;">​</a></h3><ul><li><strong>INNER JOIN</strong>：只保留两端都匹配的行</li><li><strong>LEFT JOIN</strong>：保留左表全部，右表对不上就是 NULL</li><li><strong>RIGHT JOIN</strong>：保留右表全部</li><li><strong>FULL OUTER JOIN</strong>：MySQL 没有原生语法，用 <code>LEFT JOIN UNION RIGHT JOIN</code> 模拟</li><li><strong>CROSS JOIN</strong>：笛卡尔积。<code>FROM left_table, right_table</code> 不写 <code>ON</code> 就是它</li></ul><p>过滤条件写在 <code>ON</code> 还是 <code>WHERE</code>，对左连接影响很大。<code>WHERE</code> 里写右表列 <code>IS NOT NULL</code>，等价于把左连接滤成内连接。</p><h3 id="in-与-exists" tabindex="-1">IN 与 EXISTS <a class="header-anchor" href="#in-与-exists" aria-label="Permalink to &quot;IN 与 EXISTS&quot;">​</a></h3><p>旧经验是：内表小用 <code>IN</code>（先物化内表），外表小、内表大用 <code>EXISTS</code>（外表驱动，内表点查）。现在优化器经常把 <code>IN</code> 子查询改写成 semi-join，两者差别没那么绝对，以 <code>EXPLAIN</code> 为准。</p><p>仍然成立的点：</p><ul><li><code>NOT IN</code> 遇到 NULL 会得到空结果（三值逻辑）。优先 <code>NOT EXISTS</code> 或 <code>LEFT JOIN ... WHERE right.id IS NULL</code></li><li><code>IN</code> 列表本身太大也会慢，可改 JOIN</li></ul><h3 id="char、varchar-与-int-10" tabindex="-1">CHAR、VARCHAR 与 INT(10) <a class="header-anchor" href="#char、varchar-与-int-10" aria-label="Permalink to &quot;CHAR、VARCHAR 与 INT(10)&quot;">​</a></h3><p><code>CHAR</code> 定长，短了补空格（检索时去掉尾部空格），长度几乎不变、频繁原地更新时可以考虑。<code>VARCHAR</code> 变长，多 1~2 字节长度头，更省空间，多数列的默认选择。</p><p><code>CHAR</code> 上限 255 字符；<code>VARCHAR</code> 上限在 65535 字节量级，还要扣掉行格式、NULL 标志和其他列。<code>utf8mb4</code> 下单列上限会更小。</p><p><code>INT(10)</code> 仍然是 4 字节整数，<code>10</code> 只影响 <code>ZEROFILL</code> 时左边补零显示。8.0 已废弃显示宽度。<code>CHAR(10)</code> / <code>VARCHAR(10)</code> 才是最多 10 个字符的存储长度。</p><h3 id="drop、delete、truncate" tabindex="-1">DROP、DELETE、TRUNCATE <a class="header-anchor" href="#drop、delete、truncate" aria-label="Permalink to &quot;DROP、DELETE、TRUNCATE&quot;">​</a></h3><table tabindex="0"><thead><tr><th></th><th>DROP</th><th>DELETE</th><th>TRUNCATE</th></tr></thead><tbody><tr><td>类型</td><td>DDL</td><td>DML</td><td>DDL（实现上整表重建）</td></tr><tr><td>内容</td><td>表结构 + 数据 + 索引都没了</td><td>可带 WHERE 删行，结构还在</td><td>清空全部行，结构还在</td></tr><tr><td>回滚</td><td>不能</td><td>事务里可以</td><td>一般不能（隐式提交）</td></tr><tr><td>速度</td><td>快</td><td>逐行、写 undo，慢</td><td>快</td></tr><tr><td>自增</td><td>表没了</td><td>通常不重置</td><td>通常重置</td></tr></tbody></table><p>删部分行用 <code>DELETE</code>；清空大表用 <code>TRUNCATE</code>；不要这张表了用 <code>DROP</code>。</p><h3 id="union-与临时表" tabindex="-1">UNION 与临时表 <a class="header-anchor" href="#union-与临时表" aria-label="Permalink to &quot;UNION 与临时表&quot;">​</a></h3><p><code>UNION</code> 和 <code>UNION ALL</code> 都合并结果集。<code>UNION</code> 会去重（隐含排序或去重），<code>UNION ALL</code> 原样拼接。不需要去重就用 <code>UNION ALL</code>，少一次临时表。列数、类型要兼容；<code>ORDER BY</code> 默认只对整个 UNION 的最终结果生效，需要给子查询加括号。</p><p>优化器为排序、去重、UNION、派生表等创建的中间表叫临时表，只对当前会话可见，语句或连接结束就释放。<code>EXPLAIN</code> 里出现 <code>Using temporary</code>、<code>DISTINCT</code> 搭配另一套 <code>ORDER BY</code>、<code>GROUP BY</code> 与 <code>ORDER BY</code> 列不一致，都可能触发。</p><p>内存里用 MEMORY / TempTable，撑不住落到磁盘。MySQL 8.0 磁盘临时表不再依赖 MyISAM。大排序会拖垮磁盘，能靠索引避免 filesort 就避免。显式 <code>CREATE TEMPORARY TABLE</code> 也是会话级，断开自动删除。</p><h3 id="主键、自增-id-与-not-null" tabindex="-1">主键、自增 ID 与 NOT NULL <a class="header-anchor" href="#主键、自增-id-与-not-null" aria-label="Permalink to &quot;主键、自增 ID 与 NOT NULL&quot;">​</a></h3><p>InnoDB 必须有聚簇索引。没有主键会隐藏一个 6 字节 <code>ROW_ID</code>，既不好在业务里引用，也不方便按行更新删除。</p><p>聚簇索引按主键排列，<strong>自增或有序雪花</strong>插入是追加，页分裂少。UUID 随机，插入在页中间乱跳，二级索引叶子还要带着这个长键。</p><p>自增短、顺序、索引友好，但会暴露量级，合表要改号。UUID 本地生成、全局唯一、合表简单，却占空间且无序。若一定要用 UUID，至少用有序版本（UUIDv7），或把 UUID 当业务键、主键仍用 <code>BIGINT</code>。单机表默认自增；分库分表再上雪花 / Leaf。</p><p>NULL 是三值逻辑里的「未知」，不是空字符串：</p><ul><li><code>COUNT(column)</code> 不计 NULL，<code>COUNT(*)</code> 计行</li><li><code>NOT IN (..., NULL)</code> 整个结果为空</li><li>比较、唯一约束、复合索引里 NULL 的语义都容易写错</li><li>InnoDB 行格式里 NULL 仍有位图开销；二级索引<strong>可以</strong>包含 NULL，并不是「B 树不存 NULL 所以索引失效」</li></ul><p>允许「没有值」的列再开 NULL；状态、金额、时间这类业务字段尽量 <code>NOT NULL</code> + 默认值。</p><h3 id="慢查询" tabindex="-1">慢查询 <a class="header-anchor" href="#慢查询" aria-label="Permalink to &quot;慢查询&quot;">​</a></h3><p>超过 <code>long_query_time</code> 的语句记入慢日志：</p><div class="language-sql vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> GLOBAL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> slow_query_log </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> GLOBAL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> long_query_time </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 秒</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> GLOBAL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> log_queries_not_using_indexes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>slow_query_log_file</code> 指定路径，<code>log_output</code> 可以是 FILE 或 TABLE。分析用 <code>mysqldumpslow</code>、pt-query-digest，再 <code>EXPLAIN</code>。</p><p>优化顺序一般是：有没有索引 → 索引有没有用上 → 是不是回表 / 排序 / 临时表 → 是不是锁等待 → 再考虑改表结构。</p><h3 id="少读、少算、深分页" tabindex="-1">少读、少算、深分页 <a class="header-anchor" href="#少读、少算、深分页" aria-label="Permalink to &quot;少读、少算、深分页&quot;">​</a></h3><p>少读：覆盖索引、避免 <code>SELECT *</code>、按时间或 ID 缩小范围、让 WHERE 用上索引。少算：能在 WHERE 滤掉的不要拿到应用再滤；加密、正则不要放在热点 SQL 里；排序列有索引，避免 filesort。加缓存、读写分离是后手，先把单条 SQL 读的页数降下来。</p><p>长难查询可以拆成多次短查询；多表 JOIN 拆开后按主键在应用层拼，分库后跨库 JOIN 本身就不该写在一条 SQL 里。先用小派生表缩小 ID 集合，再回表取宽列，就是延迟关联。</p><p><code>LIMIT 1000000, 20</code> 会扔掉前 100 万行，越往后越慢：</p><div class="language-sql vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 记住上一页最大 id</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 123456</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ORDER BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LIMIT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- 延迟关联：子查询只扫覆盖索引</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> o.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders o</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">INNER JOIN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> orders </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ORDER BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> create_time, id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LIMIT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> o</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>产品上深度翻页也应改成「下一页」，而不是跳到第 5000 页。</p><p>WHERE 不要在列上做函数、运算、隐式转换；范围列放联合索引最后；<code>OR</code> 改 <code>UNION ALL</code>，或保证两侧都能走索引；前导 <code>%</code> 的 LIKE 考虑全文索引或搜索引擎。<code>!=</code>、<code>NOT IN</code> 不是绝对不能用，但要看选择性。「WHERE 里出现 <code>!=</code> 就一定全表扫描」是过时口诀。</p><h3 id="语句为什么会慢" tabindex="-1">语句为什么会慢 <a class="header-anchor" href="#语句为什么会慢" aria-label="Permalink to &quot;语句为什么会慢&quot;">​</a></h3><p>偶尔慢：等行锁 / 间隙锁，甚至等 metadata lock（别人在 DDL）；redo 刷盘、checkpoint、刷脏页；瞬时 IO、Buffer Pool 被打满。</p><p>一直慢：没索引或有索引没用上；回表过多、filesort、临时表打磁盘；深分页、<code>SELECT *</code> 带大字段；统计信息过期，优化器选错计划（<code>ANALYZE TABLE</code>）。</p><p>用慢日志定位 SQL，用 <code>EXPLAIN</code> 看计划，用 <code>SHOW ENGINE INNODB STATUS</code> 看锁。</p><h3 id="书写顺序和逻辑执行顺序" tabindex="-1">书写顺序和逻辑执行顺序 <a class="header-anchor" href="#书写顺序和逻辑执行顺序" aria-label="Permalink to &quot;书写顺序和逻辑执行顺序&quot;">​</a></h3><p>书写顺序：</p><div class="language-sql vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT DISTINCT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> columns</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">JOIN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t2 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GROUP BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">HAVING</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ORDER BY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">LIMIT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>逻辑处理顺序更接近：</p><ol><li>FROM / JOIN（先笛卡尔，再 ON）</li><li>WHERE</li><li>GROUP BY</li><li>HAVING</li><li>SELECT</li><li>DISTINCT</li><li>ORDER BY</li><li>LIMIT</li></ol><p>所以 <code>WHERE</code> 不能用 SELECT 别名，<code>HAVING</code> 可以。MySQL 对 SELECT 别名的宽松程度因版本而异，不要依赖方言。8.0 窗口函数在 GROUP BY 之后、ORDER BY 之前求值。</p>`,56)]))}const g=i(l,[["render",t]]);export{E as __pageData,g as default};
